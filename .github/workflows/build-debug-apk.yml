name: Disabled Debug APK Workflow

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Debug APK workflow disabled. Release builds run on tag push."
